{
  "name": "hello",
  "version": "1.0.0",
  "description": "Hello world program for node.js",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "test": "echo no test specified",
    "dev": "nodemon index.js",
    "build:ui": "rm -rf dist && cd ../../p2/notes && npm run build && cp -r dist ../../p3/notes",
    "deploy:full": "npm run build:ui && git add . && git commit -m uibuild && git push"
  },
  "author": "Andrew",
  "license": "MIT",
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.19.2"
  },
  "devDependencies": {
    "@flydotio/dockerfile": "^0.5.8",
    "nodemon": "^3.1.4"
  }
}
